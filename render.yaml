services:
  - type: web
    name: ai-property-valuation-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python ai_property_valuation/train_model.py"
    startCommand: "uvicorn ai_property_valuation.api:app --host 0.0.0.0 --port $PORT"
    autoDeploy: false
